# Example environment variables. Copy to `.env.local` and fill in.
#
# This project does NOT require any environment variables to run.
# All data is stored in the browser's localStorage.
#
# If you add server-side features later (e.g., a real backend),
# put your secrets in `.env.local` and reference them from your code.